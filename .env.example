# Copy to `.env` and adjust for the `prod` profile.
# These match the docker-compose.yml services and application-prod.yml defaults.

DB_URL=jdbc:postgresql://localhost:5432/orders
DB_USERNAME=orders
DB_PASSWORD=orders

REDIS_HOST=localhost
REDIS_PORT=6379
